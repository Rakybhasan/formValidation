<?php

// Assignment Operators

 // =  is assigning a data to a variable

 // +=   $x += $y = $x + $y;
 // -=
 // /=
 // %=
 // *=


 // Comparison Operator


  // ==  Boolean   if($a == $b){true}:{false};
  // === The server will chek its data types; if($a===$b){true}:{false}
  //  >  Greater than if($a> $b){true}:{false};
  // <  Less than  if($a<$b){true}:{false};
  // >=  Greater or equal to if($a >= $b){true}:{false};
  // <= Less than or equal to if($a <= $b){true}:{false};
  // != Not equalto if($a != $b){true}:{false};


  // Logical Operator

  $a = 10;
  $b = 10;

  $c =20;
  $d=30;

   // AND  and  &&  if($a==$b && $c == $d) {true}:{false};
   // OR or  ||  if($a==$b || $c == $d) {true}:{false};
   // NOT not !  if($a != $b) {true}:{false};


   // Increment & Decrement

  $a = 100;

  echo ++$a . "<br>"; // Pre Increment
  echo $a++ . "<br>"; // Post Increment
  echo --$a . "<br>"; // Pre Decrement
  echo $a-- . "<br>"; // Post Decrement

















 ?>
